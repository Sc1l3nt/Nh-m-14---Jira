import React from 'react'
import { Outlet } from 'react-router-dom'

type Props = {}

const LoginTemplateMobile = (props: Props) => {
    return (
        <Outlet/>
    )
}

export default LoginTemplateMobile